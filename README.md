# 安装须知

---

### 安装 Ecshop

服务器访问 `*/ecshop/` 路径,完成 Ecshop 的安装

### 安装 Ectouch

安装成功 Ecshop 的基础上.访问路径 `*/ecshop/mobile/` 路径,完成 Ectouch 的安装.

注意:MySQL 环境太高会导致 Ectouch 无法安装,经测试 MySQL 5.5 跟 MySQL 5.6 都可以.